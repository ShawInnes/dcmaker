  minio:
    image: quay.io/minio/minio
<% if (props.exposePorts) { -%>
    ports:
      - "9000:9000"
      - "9001:9001"
<% } -%>
    environment:
      MINIO_ROOT_USER: xxxx
      MINIO_ROOT_PASSWORD: xxxx
    command: server --console-address ":9001" /data
